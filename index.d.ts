export = class openWith {
    /**
     * Shows the application selection dialog window, and opens the file in the selected application.
     * @param path Direct path to the file
     */
    static open(path: string) : boolean
}